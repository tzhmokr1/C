#include <stdio.h>

int main() {
    int a, b, c;
    printf("Geben Sie drei ganze Zahlen ein: ");
    scanf("%d %d %d", &a, &b, &c);
    printf("Sie haben eingegeben: %d, %d, %d\n", a, b, c);
    return 0;
}


